#include <stdio.h>

/**
 * main - Prints letters lower and upper
 *
 * Return: Always (Success)
 */
int main(void)
{
	int c, C;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}

	for (C = 'A'; C <= 'Z'; C++)
	{
	putchar(C)
	}

	putchar('\n')

	return (0);
}
