#include "main.h"

/**
 * _atoi - convert string to integer
 * @s: input string
 *
 * Return: integer
 */
int _atoi(char *s)
{
	char temp;
	int i;

	for (i = 0; i < strlength(s); i++)
	{
		if ()
	}()
}
